# lmtt (Linux Matugen Theme Toggle) - Generated files
# All files generated by the theme system are prefixed with 'lmtt-'
**/lmtt-*

# Old software installers (migrated to run_after scripts)
software_installers/
software_installers.backup-*/

# Installation logs
**/*.log

# Plan files
.plans/
CURRENT_PLAN.md

# Test infrastructure
tests/
.github/

# Python cache
**/__pycache__/
**/*.pyc
.config/fish/fish_variables

# Chezmoi Agent files
AGENTS.md
CLAUDE.md

# Repo documentation (stays in repo, not deployed to home)
README.md
LICENSE
docs/

# Repo tooling (stays in repo, not deployed to home)
.githooks
.githooks/**
.gitleaks.toml

# Beads issue tracking (stays in repo, not applied to home)
.beads/

# Git configuration (stays in repo)
.gitattributes

# btop rewrites its own config on startup (version line, boolean casing) so
# tracking it just produces churn we'd never apply back.
.config/btop/btop.conf

# hyprstate manages this symlink at runtime based on connected monitors.
# Tracking it would pin the wrong profile on other machines.
.config/hypr/profiles/.active.lua

# Monitor-profile .lua files are BUILD ARTIFACTS: hyprstate regenerates them
# from the sibling .toml (`hyprstate profile migrate`, and on demand during
# `profile list`/`switch`), flattening mon.row into explicit hl.monitor calls
# and dropping comments. The .toml is the source of truth — track that, and put
# explanatory prose in it as `#` comments so regeneration can't eat it.
.config/hypr/profiles/*.lua

# lmtt regenerates this on every theme switch from the embedded template in
# .config/lmtt/modules/mako.toml — that .toml is the source of truth.
.config/mako/config

# Claude templates live in the private work overlay repo (deployed on work
# machines by run_onchange_after_35-sync-work-overlay), never in this public
# repo — so chezmoi must not manage this path on any machine.
.claude/templates

# App-owned settings: Claude Code and Zed rewrite these constantly (model
# changes, plugin toggles, panel layout), so managing them is perpetual drift.
.claude/settings.json
.config/zed/settings.json

# Gaming/Steam: personal machines only.
{{ if .is_work }}
.config/hypr/scripts/bp-game-focus.py
.config/hypr/scripts/steam-clean-shutdown.sh
.config/environment.d/gaming.conf
.local/bin/steam-set-launch-options
.local/share/notes
.local/share/notes/**
{{ end }}

# Work app entries
{{ if not .is_work }}
.local/share/applications/slack.desktop
.local/share/applications/clickup-icon.png
{{ end }}

# Steam Big Picture => ultrawide only. Desktop-only; the module also no-ops
# on any machine without that monitor.
{{ if ne .chezmoi.hostname "mason-desktop" }}
.config/hypr/bp-monitors.lua
{{ end }}

# Fresh-machine bootstrap (run from curl, stays in repo)
bootstrap.sh
.config/gtk-3.0/settings.ini
.config/gtk-4.0/settings.ini
.config/hypr/hyprtoolkit.conf

# mcp-hub config: deliberately untracked (public repo; contains work email/paths)
.config/mcphub
.config/mcphub/**

# Headless machines (SSH only — see `headless` in .chezmoi.toml.tmpl): no
# desktop session, no keyring, no session bus. What is left is the shell,
# git, ssh, nvim, tmux and the Claude config.
{{ if dig "headless" false . }}
# Desktop session and GUI apps
.config/hypr
.config/hypr/**
.config/hypr-de
.config/hypr-de/**
.config/lmtt
.config/lmtt/**
.config/uwsm
.config/uwsm/**
.config/wireplumber
.config/wireplumber/**
.config/xdg-desktop-portal
.config/xdg-desktop-portal/**
.config/kitty
.config/kitty/**
.config/wezterm
.config/wezterm/**
.config/zed
.config/zed/**
.config/fontconfig
.config/fontconfig/**
.config/gtk-3.0
.config/gtk-3.0/**
.config/gtk-4.0
.config/gtk-4.0/**
.config/networkmanager-dmenu
.config/networkmanager-dmenu/**
.config/mimeapps.list
.config/chromium-flags.conf
.config/discord-flags.conf
.config/electron-flags.conf
.config/software-manifest.md
.local/share/applications
.local/share/applications/**
.local/share/icons
.local/share/icons/**
.local/share/notes
.local/share/notes/**
.blerc
# Login shell is bash; fish config is desktop-era and keyring-coupled.
.config/fish
.config/fish/**
# Session environment for a graphical login; SSH shells never read it.
.config/environment.d
.config/environment.d/**
# User units: all desktop/keyring services. herdr.service on a headless box
# is owned by whatever provisions it (home-lab proxmox/dev/bootstrap.sh), and
# dev.slice exists to protect a desktop from agents — on a dedicated box it
# would only throttle the one thing the box is for.
.config/systemd
.config/systemd/**
.bashrc.d/dev-slice.bashrc
# Shell modules that assume a VT, a compositor or a GUI terminal. ssh.bashrc
# would spawn one ssh-agent per shell here (no systemd agent socket in an SSH
# login), and a headless box uses its own passphrase-less key directly.
.bashrc.d/00-tty-console.bashrc
.bashrc.d/05-uwsm-env.bashrc
.bashrc.d/wezterm-shims.bashrc
.bashrc.d/ydotool.bashrc
.bashrc.d/android.bashrc
.bashrc.d/cursor.bashrc
.bashrc.d/ssh.bashrc
# Desktop helper binaries and scripts; nvim-open and cb still make sense.
.local/bin/browser-default-guard
.local/bin/buzz-appimage-patch
.local/bin/buzz-desktop
.local/bin/claude-desktop-update
.local/bin/import-api-keys
.local/bin/list-session-env
.local/bin/networkmanager-dmenu
.local/bin/slack-mcp
.local/bin/steam-set-launch-options
.local/bin/swiftpoint-profile-osd
.local/bin/chezmoi-refresh-identity
scripts/terminal-header
scripts/terminal-header/**
scripts/*wezterm*
scripts/terminal-session-task-stack.ts
scripts/thermal-tripwire
scripts/chezmoi-package-check.py
scripts/swappy-*.md
nix-environments
nix-environments/**
# Scripts: everything that configures hardware, a desktop session, a keyring
# or this repo's push path. Left to run: jq, the bash login shell check.
.chezmoiscripts/05-setup-bitwarden.sh
.chezmoiscripts/05-install-fonts.sh
.chezmoiscripts/34-claude-cowork-vm.sh
.chezmoiscripts/35-sync-work-overlay.sh
.chezmoiscripts/40-setup-git-hooks.sh
.chezmoiscripts/50-nm-wifi-powersave.sh
.chezmoiscripts/51-fw16-grub-cmdline.sh
.chezmoiscripts/52-fw16-mt7922-wifi.sh
.chezmoiscripts/53-system-session-tweaks.sh
.chezmoiscripts/54-bluetooth-tweaks.sh
.chezmoiscripts/54-third-party-repos.sh
.chezmoiscripts/55-tty-console.sh
.chezmoiscripts/98-check-packages-with-claude.sh
.chezmoiscripts/99-track-system.sh
.chezmoiscripts/remove-root-wezterm-config.sh
enable-user-services.sh
install-chromium-conf.sh
{{ end }}
