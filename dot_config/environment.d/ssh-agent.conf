# SSH agent socket — use the systemd-user-managed socket so every process in
# the session (including bash subshells / Claude Code) sees the agent, not
# just fish. Moved from ~/.config/fish/conf.d/ssh-agent.fish.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
