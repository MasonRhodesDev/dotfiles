# Global Notes

## Mermaid/PDF Generation
- Pipeline: `mmdc -e pdf` → fix captions → `pandoc --pdf-engine=xelatex`
- Only `mmdc -e pdf` (Puppeteer) renders mermaid text correctly — cairosvg/inkscape cannot
- See `~/.claude/references/mermaid-pdf.md` for full pandoc options, fonts, and settings

## Git
- Never add Co-Authored-By lines for Claude on commits.
