# Enable mouse and vim bindings
set -g mouse on
setw -g mode-keys vi

# Enable terminal hyperlink support
set -as terminal-features ",*:hyperlinks"

# Load dynamic theme colors from lmtt (Linux Matugen Theme Toggle)
source-file -q ~/.config/tmux/lmtt-theme.conf
