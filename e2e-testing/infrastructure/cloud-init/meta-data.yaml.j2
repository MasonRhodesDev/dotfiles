# Cloud-init meta-data template

instance-id: {{ vm_name }}
local-hostname: {{ vm_name }}

# Network configuration
network:
  version: 1
  config:
    - type: physical
      name: eth0
      subnets:
        - type: dhcp