Add Claude Code module and performance monitoring system

Features:
- Claude Code theme module with local installation detection
- Parallel module execution for maximum performance
- Performance monitoring with 0.25s threshold warnings
- Detailed timing analysis for each module
- Visual performance indicators (✓ fast, ⚠️ slow)

Claude Code module:
- Detects both global and local (~/.claude/local/claude) installations
- Uses claude config commands for theme switching
- Smart caching to avoid redundant theme changes

Performance system:
- All modules run in parallel background processes
- Real-time timing with nanosecond precision
- Automatic alerts for modules exceeding 0.25s threshold
- Helps identify optimization opportunities

Measured performance:
- GTK: ~0.02s (environment variables)
- WezTerm: ~0.22s (Python config generation)
- Wofi: ~0.22s (CSS generation)
- Tmux: ~0.38s (regex processing + reload)
- Claude Code: ~1.7s (config command overhead)
- HyprPanel: ~2.1s (restart process)

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>
