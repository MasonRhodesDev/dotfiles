local M = {
  "typicode/bg.nvim",
  lazy = false, -- Load immediately to sync terminal background
}

-- bg.nvim automatically syncs terminal background with colorscheme
-- No additional configuration needed - it works out of the box

return M