# The ~20s whisper model load lands right in the login fan-out and competes
# with waybar/portals/compositor for CPU and IO (2026-08-06). Idle-priority it:
# only bites under contention; steady-state dictation latency is unaffected.
[Service]
Nice=10
IOSchedulingClass=idle
