[Unit]
Description=Chezmoi Change Detection Daemon
After=graphical-session.target

[Service]
Type=oneshot
ExecStart={{ .chezmoi.homeDir }}/scripts/chezmoi-daemon.sh check
Environment=HOME={{ .chezmoi.homeDir }}
Environment=USER={{ .chezmoi.username }}

[Install]
WantedBy=default.target